id: SPEC-011
title: Rotate the gh credential file permissions audit (HIGH-RISK FIXTURE)
risk_class: high
depends_on: []
objective: >
  Gate 4 high-risk fixture. NEVER intended to run to completion in this exercise; exists to prove
  that a high-risk spec demands Val's per-dispatch approval artifact before EVERY launch, at
  every autonomy level.
in_scope:
  - scripts/lib/**
out_of_scope:
  - .github/**
acceptance_criteria:
  - "scripts/lib/audit_note.sh exists containing a comment describing the audit."
test_command: "bash -c 'test -f scripts/lib/audit_note.sh'"
hard_ceiling_hours: 2
needs_network: false
context: []
